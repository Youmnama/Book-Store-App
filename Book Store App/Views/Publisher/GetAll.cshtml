@model IEnumerable< Book_Store_App.Models.Domain.Puplisher>
@{
     
}

<div class="container pt-5">
    <div class="row">
        <div class="col-md-6">
            <a class="btn btn-dark" href="/Genre/Add"> Add More </a>

        </div>

    </div>
    <div class="row">
        <div class="col-md-6">
            <h3>Add Genre</h3>
            <table class="table table-striped table-bordered table-dark">

                <thead>
                    <tr>
                        <th> Publisher Name</th>
                        <th>Action</th>
                    </tr>

                </thead>
                <tbody>
                    @foreach(var item in Model)
                    {
                        <tr>
                            <td>@item.PuplisherName</td>
                            <td>
                                <a class="btn btn-success" href="/Genre/Edit?id =@item.Id">Edit</a>

                                <a class="btn btn-danger" onclick="return window.confirm('Are You Sure?')" href ="/Genre/Delete?id =@item.Id">Delete</a>
                            </td>

                        </tr>
                    }

                </tbody>
            </table>
            

        </div>

    </div>
        
</div>

